/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'via.placeholder.com',
      'api.tv8282.com',
      'tinypic.host'
    ],
  },
}

module.exports = nextConfig